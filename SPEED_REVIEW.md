# edu_pipeline 속도 최적화 점수표

> 검토일: 2026-03-25
> 환경: RTX 4060 Ti 8GB / Python 3.10.6 / CUDA 12.4
> 기준: 10분 영상, ~60 세그먼트

---

## 종합 속도 점수: 52 / 100

> 현재 10분 영상 처리에 **~3분** 소요 추정.
> 96%가 GPU 바운드이므로, CPU 최적화는 의미 없고 **GPU 활용 효율**이 핵심.

---

## 시간 분포 (10분 영상 기준)

```
STEP 1: 오디오 추출 (ffmpeg)              3.0s   ██
STEP 2: STT (Whisper large-v3-turbo)    40.0s   ██████████████████████
STEP 3: LLM 로드                        15.0s   ████████
STEP 3: LLM 번역 (6 배치)               45.0s   █████████████████████████
STEP 3: LLM 요약 x3                     20.0s   ███████████
STEP 4: TTS 로드                         8.0s   ████
STEP 4: TTS 합성 (60 seg, nfe=16)       45.0s   █████████████████████████
STEP 4: 시간 스트레칭 (CPU, overlap)      1.2s   █
STEP 4: 더빙 합성 (ffmpeg)               3.0s   ██
STEP 5: Embedding 로드                    5.0s   ███
STEP 5: Embedding 추론                    3.0s   ██
STEP 6: ChromaDB 인덱싱                   0.2s
                                       ───────
                                       188.4s  (~3.1분)
```

### 병목 TOP 3

| 순위 | 단계 | 시간 | 비중 | 원인 |
|------|------|------|------|------|
| 1 | LLM 번역+요약 | 80s | 42% | 4-bit이지만 autoregressive 생성 자체가 느림 |
| 2 | TTS 합성 | 45s | 24% | 60개 세그먼트 × nfe_step=16 |
| 3 | STT | 40s | 21% | beam_size=5 + VAD |

**이 3개가 전체의 87%를 차지합니다.**

---

## 파일별 속도 점수

### `main.py` — 55/100

| 항목 | 점수 | 이유 |
|------|------|------|
| 파이프라인 순차 실행 | 5/10 | 모든 단계가 직렬. 독립적인 단계(번역 vs 요약)도 순차 실행 |
| 모델 로드/해제 오버헤드 | 4/10 | 5개 모델을 순차 로드/해제. 로드만 합쳐 ~43s (전체의 23%) |
| lazy import | 8/10 | 무거운 모듈 지연 로드 — CLI 시작은 빠름 |
| 요약 3회 호출 | 4/10 | child/teen/adult 각각 별도 LLM 호출. 배치로 묶으면 1회로 줄일 수 있음 |

**개선 가능:**
- 요약 3개를 하나의 프롬프트로 합쳐 1회 호출로 줄이면 ~13초 절약
- 번역과 요약을 하나의 LLM 세션에서 처리 (이미 하고 있음 — OK)

### `modules/stt.py` — 65/100

| 항목 | 점수 | 이유 |
|------|------|------|
| faster-whisper 선택 | 9/10 | CTranslate2 백엔드로 원본 Whisper 대비 4배 빠름 |
| large-v3-turbo 모델 | 8/10 | v3 대비 40% VRAM 절감 + 2-3배 속도 향상 |
| beam_size=5 | 5/10 | beam=1 (greedy) 대비 ~2배 느림. 한국어에서 beam=3이면 충분 |
| float16 | 8/10 | 최적 compute type |
| 병렬 처리 | 3/10 | 단일 스레드 순차 처리. Whisper는 내부적으로 30초 윈도우지만 배치 불가 |

**개선 가능:**
- `beam_size=5` → `3`으로 줄이면 STT 시간 ~30% 감소 (40s → 28s)
- `best_of=1`은 이미 최적

### `modules/llm.py` — 40/100 (가장 느린 구간)

| 항목 | 점수 | 이유 |
|------|------|------|
| 4-bit 양자화 | 8/10 | NF4 + double_quant — 메모리 대비 최적 |
| 배치 번역 (batch_size=10) | 5/10 | 한 번에 10문장 묶어 번역 — 좋은 시도. 하지만 autoregressive 생성이라 입력 길이에 비례해 느려짐 |
| 요약 3회 독립 호출 | 3/10 | 동일 텍스트를 3번 읽고 3번 생성. 프롬프트 한 번에 3개 수준 요약을 한꺼번에 요청하면 1회로 가능 |
| KV cache 비활용 | 2/10 | 매 호출마다 전체 프롬프트를 다시 인코딩. transformers의 `past_key_values` 미사용 |
| 추론 엔진 | 3/10 | 순수 transformers generate(). vLLM, llama.cpp, TensorRT-LLM 대비 2-5배 느림 |
| `max_new_tokens=256*n` | 6/10 | batch_size=10일 때 2560 토큰 할당. 실제 출력은 ~500 토큰이므로 EOS에서 조기 종료되긴 함 |

**개선 가능:**
- 요약 1회 호출 통합: ~13초 절약
- vLLM/llama.cpp 서빙: 번역+요약 시간 50-70% 감소 가능 (하지만 아키텍처 변경 필요)
- `batch_size=10` → `15-20`으로 늘리면 호출 횟수 감소 (VRAM 여유 있으면)

### `modules/tts.py` — 70/100

| 항목 | 점수 | 이유 |
|------|------|------|
| GPU/CPU 오버랩 | 9/10 | ThreadPoolExecutor로 GPU 추론과 CPU 스트레칭 병렬화 — **이 코드에서 가장 좋은 최적화** |
| nfe_step=16 | 7/10 | 32 대비 2배 빠름. 8이면 4배 빠르지만 품질 저하. 16은 합리적 균형점 |
| 세그먼트별 개별 추론 | 4/10 | 60개 세그먼트를 하나씩 추론. F5-TTS는 배치 추론 미지원이므로 불가피 |
| tempfile I/O | 6/10 | 매 세그먼트마다 임시 파일 생성/삭제. numpy 배열 직접 전달하면 제거 가능하나 stretch 함수가 파일 입력 |

**개선 가능:**
- 짧은 세그먼트(<1초)는 스트레칭 스킵 — 이미 `abs(ratio-1.0)<0.02` 체크 있음
- 스트레칭을 numpy 배열 기반으로 바꾸면 tempfile I/O 제거 (세그먼트당 ~2ms 절약 × 60 = ~120ms)

### `modules/embedder.py` — 80/100

| 항목 | 점수 | 이유 |
|------|------|------|
| batch_size=32 | 8/10 | 60개 세그먼트를 2배치로 처리 — 효율적 |
| float16 | 8/10 | 속도와 VRAM 절감 균형 |
| 모델 크기 (0.6B) | 9/10 | 초경량. 로드 ~5초, 추론 ~3초 — 전체의 4%에 불과 |
| 벡터 캐싱 없음 | 6/10 | 동일 텍스트 재임베딩 시 중복 계산. 파이프라인에서는 1회만 호출이라 영향 적음 |

**거의 최적화 완료. 개선 여지 적음.**

### `modules/rag.py` — 45/100

| 항목 | 점수 | 이유 |
|------|------|------|
| ChromaDB 검색 속도 | 9/10 | 1ms/query — 사실상 즉시 |
| 모델 교차 로드 (chat 모드) | 3/10 | 질문마다 Embedder 로드(5s) → 해제 → LLM 로드(15s) → 해제 = **질문당 ~25초 오버헤드** |
| 인덱싱 | 8/10 | upsert 500개 0.14초 — 충분히 빠름 |

**개선 가능:**
- GGUF 양자화 모델 + llama-cpp-python으로 LLM 서빙하면 로드 시간 15s → 2s
- 또는 Embedder/LLM을 동시 로드할 수 있는 GPU 메모리가 있으면 교차 로드 불필요

### `utils/audio.py` — 75/100

| 항목 | 점수 | 이유 |
|------|------|------|
| ffmpeg subprocess | 7/10 | 프로세스 스폰 오버헤드 ~40ms/call. PyAV 사용하면 인프로세스로 더 빠름 |
| `-c:v copy` | 9/10 | 비디오 재인코딩 안 함 — 더빙 합성이 빠른 핵심 |
| 16kHz 모노 변환 | 8/10 | STT에 필요한 최소 품질 — 불필요한 데이터 없음 |

### `utils/timestamp_sync.py` — 75/100

| 항목 | 점수 | 이유 |
|------|------|------|
| librosa time_stretch | 7/10 | phase vocoder 기반. rubberband 대비 느리지만 추가 설치 불필요 |
| n_fft=1024 | 9/10 | n_fft=512 대비 **28% 빠름** + 음질 향상 (프레임 수 감소) |
| 비율 근사 스킵 | 9/10 | `abs(ratio-1.0)<0.02`면 스트레칭 안 함 — 불필요한 연산 회피 |
| TTS와 CPU 오버랩 | 8/10 | TTS의 ThreadPoolExecutor에서 비동기 실행 — GPU와 병렬 |

### `utils/memory.py` — 85/100

| 항목 | 점수 | 이유 |
|------|------|------|
| `empty_cache()` + `synchronize()` | 9/10 | VRAM 즉시 해제 보장 |
| `gc.collect()` in finally | 8/10 | 파이썬 순환 참조 해제 |
| 로그 오버헤드 | 7/10 | `log_vram()`이 매번 `torch.cuda.memory_allocated()` 호출 — 미세하지만 불필요한 GPU 동기화 유발 |

---

## 최적화 난이도별 로드맵

### Tier 1 — 코드만 수정 (구조 변경 없음, 즉시 적용 가능)

| 개선 | 예상 절약 | 난이도 |
|------|-----------|--------|
| STT `beam_size=5` → `3` | ~12초 (STT 30% 감소) | config 1줄 |
| 요약 3회 → 1회 통합 프롬프트 | ~13초 (요약 65% 감소) | llm.py 수정 |
| **합계** | **~25초 (전체 13% 감소)** | |

### Tier 2 — 중간 수준 변경

| 개선 | 예상 절약 | 난이도 |
|------|-----------|--------|
| LLM을 llama-cpp-python(GGUF)으로 교체 | 번역+요약 40-50% 감소 (~35초) | 모듈 교체 |
| TTS nfe_step=16 → 12 | ~10초 (TTS 25% 감소) | 음질 트레이드오프 |
| timestamp_sync를 numpy 직접 처리 | tempfile I/O 제거 (~0.5초) | 미미 |
| **합계** | **~45초 (전체 24% 감소)** | |

### Tier 3 — 구조적 변경

| 개선 | 예상 절약 | 난이도 |
|------|-----------|--------|
| vLLM 서빙 (continuous batching) | 번역+요약 60-70% 감소 (~55초) | 아키텍처 변경 |
| Whisper → Distil-Whisper | STT 50% 감소 (~20초) | 모델 교체 |
| TTS 세그먼트 병합 후 일괄 생성 | TTS 30% 감소 (~15초) | 로직 변경 |
| **합계** | **~90초 (전체 48% 감소)** | |

---

## 이론적 최적 시간 (모든 Tier 적용 시)

```
현재:       188초 (~3.1분)
Tier 1:     163초 (~2.7분)  — 즉시 적용 가능
Tier 1+2:   118초 (~2.0분)  — 중간 작업
Tier 1+2+3:  73초 (~1.2분)  — 대규모 변경
```

---

## 결론

| 관점 | 점수 | 설명 |
|------|------|------|
| **현재 속도** | 52/100 | 10분 영상에 3분 — 실시간의 3.3배. 나쁘진 않지만 최적과는 거리 있음 |
| **최적화 노력 대비 효율** | 70/100 | GPU/CPU 오버랩, 4-bit 양자화, faster-whisper 등 기본적인 것은 잘 해놓음 |
| **병목 인식** | 40/100 | LLM이 42%를 차지하는데 transformers 순수 generate 사용. 가장 큰 병목에 대한 최적화가 약함 |
| **확장성** | 35/100 | 1시간 영상이면 ~18분. 배치 처리/스트리밍 미지원 |
